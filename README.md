# Frontend del Proyecto final

# El gestor de paquetes no es NPM es BUN, mucho mas rapido en ejecucion, asi que si no tiene por favor ejecutar este comando 

# powershell -c "irm bun.sh/install.ps1 | iex" o curl -fsSL https://bun.sh/install | bash si estan en linux